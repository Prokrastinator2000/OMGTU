using System;
class HelloWorld {
  static void Main() {
    int kolvo=0;
    for (int i=0;i<=9;i++)
    {
        int n=Convert.ToInt32(Console.ReadLine());
        if (n<0)
        {
            kolvo=kolvo+1;
            
        }
    }
  Console.WriteLine(kolvo);
      
  }
}
