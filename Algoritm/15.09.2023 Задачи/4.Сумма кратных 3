using System;
class HelloWorld {
  static void Main() {
    int sum=0;
    for (int i=0;i<=9;i++)
    {
        int n=Convert.ToInt32(Console.ReadLine());
        if (n%3==0)
        {
            sum+=n;
        }
    }
 Console.WriteLine(sum);
  }
}
