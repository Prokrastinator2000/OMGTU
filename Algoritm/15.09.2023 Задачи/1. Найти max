using System;
class HelloWorld {
  static void Main() {
    int n=Convert.ToInt32(Console.ReadLine());
    for (int i=0;i<=8;i++)
    {
        int max=Convert.ToInt32(Console.ReadLine());
        if (max>n)
        {
            n=max;
        }
    }
 Console.WriteLine(n);
  }
}
